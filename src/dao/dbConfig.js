//import mongoose from 'mongoose';
//import configs from '../config/config.js';

// Conectar a la base de datos MongoDB utilizando Mongoose
//async function connectToDatabase() {
//    try {
//        // Establecer conexión utilizando la URL de conexión desde la configuración
//        await mongoose.connect(configs.mongoUrl);
//        console.log('Base de datos conectada exitosamente.');
//    } catch (error) {
        // Capturar y registrar cualquier error que ocurra durante la conexión
//        console.log('Error al conectar con la base de datos:', error);
//    }
//}

// Llamar a la función para establecer la conexión cuando el módulo se ejecute
//connectToDatabase();
