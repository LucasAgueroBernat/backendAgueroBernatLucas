<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            background-color: #000;
            color: #fff;
            font-family: 'Roboto', sans-serif;
            text-align: center;
        }
        h2 {
            font-size: 2em;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #FFD700; /* Amarillo */
        }
        label {
            display: block;
            font-size: 1.2em;
            margin-bottom: 10px;
            color: #FFD700; /* Amarillo */
        }
        input[type="text"] {
            padding: 10px;
            margin-bottom: 20px;
            width: 300px;
            border: 2px solid #FFD700; /* Amarillo */
            border-radius: 5px;
            background-color: #000;
            color: #fff;
        }
        input[type="submit"] {
            padding: 10px 20px;
            background-color: #FFD700; /* Amarillo */
            border: none;
            border-radius: 5px;
            font-size: 1.2em;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        input[type="submit"]:hover {
            background-color: #ffd70080; /* Amarillo con transparencia */
        }
    </style>
</head>
<body>
    <h2>New password</h2>
    <div>
        <form id="SendPasswordForm">
            <label>Your new password:</label>
            <input type="password" name="password_reset">
            <br>
            <input type="submit" value="Enviar">
        </form>
    </div>
</body>
</html>
