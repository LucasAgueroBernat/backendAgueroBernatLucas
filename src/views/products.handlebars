<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos - Star Wars</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #000;
            color: #fff;
        }
        h1 {
            font-size: 36px;
            text-align: center;
            margin-top: 20px;
        }
        h2 {
            font-size: 24px;
            margin-bottom: 5px;
        }
        p {
            font-size: 18px;
            margin: 5px 0;
        }
        img {
            width: 200px;
            height: auto;
            margin-bottom: 10px;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        .welcome-container {
            text-align: center;
            margin-bottom: 20px;
        }
        .products-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }
        .product {
            text-align: center;
            margin: 20px;
            padding: 20px;
            background-color: #111;
            border-radius: 10px;
        }
        .pagination {
            text-align: center;
            margin-top: 20px;
        }
        .pagination a {
            color: #fff;
            text-decoration: none;
            margin: 0 5px;
        }
        .pagination a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>PRODUCTOS </h1>
        <div class="welcome-container">
            <h1>BIENVENIDO</h1>
            <p>Usuario: {{user.name}}</p>
            <p>Email: {{user.email}}</p>
            <p>Edad: {{user.age}}</p>
            <p>Rol: {{user.rol}}</p>
        </div>
        <div class="products-container">
            {{#each products}}
            <div class="product">
                <img src="{{this.thumbnail}}" alt="{{this.title}}">
                <h2>{{this.title}}</h2>
                <p>${{this.price}}</p>
                <a href="/products/{{this._id}}" class="view-details-btn">Ver Detalles</a>
            </div>
            {{/each}}
        </div>
        <div class="pagination">
            {{#if hasPrevPage}}
            <a href="?page={{prevPage}}&limit={{limit}}">Página Anterior</a>
            {{/if}}
            Página {{page}} de {{totalPages}}
            {{#if hasNextPage}}
            <a href="?page={{nextPage}}&limit={{limit}}">Página Siguiente</a>
            {{/if}}
        </div>
    </div>
</body>
</html>
